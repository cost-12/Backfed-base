# mkinitcpio preset file for the linux-cachyos kernel on the Backfed-base ISO
# This forces the build system to use the 'archiso' preset.

ALL_kver="/boot/vmlinuz-linux-cachyos"
ALL_config="/etc/mkinitcpio.conf"

PRESETS=('archiso')

# The 'archiso' preset uses a specific config file provided by the archiso package
# and generates the correct image name for our kernel.
archiso_config="/etc/mkinitcpio.conf.d/archiso.conf"
archiso_image="/boot/initramfs-linux-cachyos.img"
